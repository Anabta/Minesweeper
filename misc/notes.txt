Bugs:
In the settings window: when you select custom but don't fill width/height/bombcount
In the settings window: when not selecting anything but pressing ok nothing happens

ToDos:
put automatic opening of fields into another thread